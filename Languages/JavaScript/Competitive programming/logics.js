// ==============================
// カウント（Map / 配列）
// 出現回数を数える
// 同じ値をまとめる
// ==============================
const count = new Map();
for (const x of array) {
    count.set(x, (count.get(x) || 0) + 1);
}

// ==============================
// ソートして前後を見る
// 同じ値をまとめる
// ==============================
array.sort((a, b) => a - b);
for (let i = 1; i < array.length; i++) {
  if (array[i] === array[i - 1]) {
    // 同じ値
  }
}

// ==============================
// 繰り上がり（2つで1つ）
// ==============================
for (const key of keys) {
  const c = count.get(key);
  const carry = Math.floor(c / 2);
  count.set(key, c % 2);
  count.set(key * 2, (count.get(key * 2) || 0) + carry);
}

// ==============================
// 累積和
// ==============================
const ps = [0];
for (let x of array) ps.push(ps.at(-1) + x);

const sum = ps[r] - ps[l];

// ==============================
// ==============================
// ==============================
// ==============================
// ==============================
// ==============================
// ==============================
// ==============================
